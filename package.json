{
  "name": "fire-cat-chat",
  "version": "1.1.0",
  "description": "WebSocket чат с видеозвонками и push-уведомлениями",
  "scripts": {
    "vercel-build": "echo 'No build process needed' && mkdir -p public && cp sw.js manifest.json public/ && echo 'Static files ready'",
    "build": "echo 'No build process needed' && mkdir -p public && cp sw.js manifest.json public/",
    "dev": "npx serve public"
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}